# AI Assignments

This repository contains implementations of several Artificial Intelligence concepts including intelligent agents, search algorithms, and human verification systems.

---

# 1. AQI Intelligent Agent

## Description
This project implements an ***Air Quality Index (AQI) intelligent agent*** that determines the air quality level of different regions. The agent retrieves live air pollution data using the OpenWeather Air Pollution API, calculates AQI sub-indices for different pollutants, and determines the overall AQI category.

The agent processes real-time environmental data and classifies air quality into standard AQI categories such as Good, Moderate, Poor, etc.

## Data Source
The system uses the OpenWeather Air Pollution API to fetch live pollutant concentration data for specified geographic coordinates (latitude and longitude). The API provides concentrations of major pollutants such as PM2.5, PM10, NO₂, O₃, CO, SO₂, and NH₃.

These pollutant values are then used to compute AQI using standard AQI breakpoint tables.

## Components
- `code.py` – Main agent implementation
- `config.py` – Configuration settings
- `aqi_categories.csv` – AQI category definitions
- `aqi_checkpoints.csv` – AQI dataset for testing

## Agent Architecture
The AQI system follows a **Simple Reflex Agent architecture**:
1. Sensor reads AQI values.
2. Agent checks category rules.
3. Actuator outputs the air quality category.

---

# 2. Turing Test & CAPTCHA

## Turing Test

### Description
A simulation of the **Turing Test** where a human judge attempts to distinguish between responses from a machine and a human.

### Implementation
- `turing.py`

### Architecture
Components:
1. Judge (user input)
2. Human responder
3. Machine responder
4. Evaluation module

The system randomly presents responses and the judge must guess which one is generated by the machine.

---

## CAPTCHA System

### Description
A simple CAPTCHA generator that creates distorted text images and verifies user input to distinguish humans from bots.

### Implementation
- `Captcha.py`

### Features
- Random CAPTCHA text generation
- CAPTCHA image creation
- Noise lines for distortion
- User verification

### Architecture

1. CAPTCHA Generator  
2. Image Renderer  
3. User Input Module  
4. Verification Engine  
5. Human/Bot Decision

---

# 3. Water Jug Problem (Search Algorithms)

## Description
Implementation of **Uninformed Search Algorithms** to solve the classic **Water Jug Problem**.

Jugs:
- Jug 1 = 4 liters
- Jug 2 = 3 liters

Goal:
Measure exactly **2 liters** of water.

---

## Algorithms Implemented

### Breadth First Search (BFS)
- Explores level by level
- Guarantees optimal solution

### Depth First Search (DFS)
- Explores deep paths first
- Uses less memory but not always optimal

---

## Files

- `water_jug.py` – Problem definition
- `bfs.py` – BFS implementation
- `dfs.py` – DFS implementation
- `main.py` – Runs the search algorithms

---

## Performance Comparison

| Algorithm | Complete | Optimal | Memory |
|----------|----------|--------|--------|
| BFS | Yes | Yes | High |
| DFS | Yes | No | Low |

---

# Technologies Used

- Python 3
- Pillow (for CAPTCHA generation)

---

---

# How to Run

## Install Dependencies

Install the required Python libraries before running the programs.

```
pip install -r requirements.txt
```

---

## Run the AQI Intelligent Agent

```
cd AQI\ Agent
python3 code.py
```
---

## Run the Water Jug Search Algorithms

```
cd Water\ Jug
python3 main.py
```
---

## Run the Turing Test Simulation

```
cd Turing_Captcha
python3 turing.py
```
---

## Run the CAPTCHA System

```
cd Turing_Captcha
python3 Captcha.py
```
